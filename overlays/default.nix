# OSV Overlays
#
# Package overlays for OSV.
# Only add overlays when strictly necessary.
# Prefer upstream packages when possible.
{ ... }:

final: prev: {
  # Placeholder - add overlays here only when required
}
